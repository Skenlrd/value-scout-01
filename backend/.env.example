# ValueScout Backend Configuration

# Server Port
PORT=8000

# MongoDB Connection
MONGO_URI=mongodb://127.0.0.1:27017/value_scout

# Collection Name for Products
COLLECTION_NAME=products

# AI API URL (Python Flask backend)
AI_API_URL=http://localhost:5000

# Email (SMTP) Configuration
# For Gmail, use an App Password (recommended) and keep these values out of git.
EMAIL_USER=valuescout6@gmail.com
EMAIL_PASSWORD=bire mumg gliz bwka
# Optional, but recommended (controls the From header)
EMAIL_FROM=ValueScout <valuescout6@gmail.com>
# Optional overrides
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false

# Optional: MongoDB Atlas Connection
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/value_scout?retryWrites=true&w=majority

# Optional: Node Environment
NODE_ENV=development
